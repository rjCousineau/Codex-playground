# Example Codex Prompts

## Add a feature
Update `scripts/hello.js` so it also supports a `--upper` flag.

## Add tests
Create tests for default greeting, custom name, and uppercase mode.

## Refactor
Refactor the script to separate argument parsing from greeting logic.

## Improve docs
Update the README with usage examples.
